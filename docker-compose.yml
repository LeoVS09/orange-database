version: "3"

services:

   orange-database:
      image: orange-database/0.1.0
      container_name: orange-database
      ports:
         - 8765:8765
      volumes:
         - .:/ws/.
      env_file:
         - .env-list
      depends_on:
        - db
      links:
        - db
#      networks:
#         - database

   db:
      image: postgres:11
      container_name: postgres
      volumes:
         - ./postgres-data:/var/lib/postgresql/data
      ports:
         - 5432:5432
#      networks:
#         - database

# TODO: Use network in production
#networks:
#  database:
#   driver: bridge
